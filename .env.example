# SentinelOne API Configuration
# Get your API key from: Settings > Users > [Your User] > API Token

# Required: Your SentinelOne API key
SENTINELONE_API_KEY=your_api_key_here

# Required: Your SentinelOne management console URL (no trailing slash)
# Example: https://usea1-purple.sentinelone.net
SENTINELONE_API_BASE=https://your-instance.sentinelone.net

# MCP Transport Configuration (optional)
# Transport mode: "stdio" (default, for Claude Code) or "http" (for remote MCP connectors)
MCP_TRANSPORT=stdio

# HTTP server port (only used when MCP_TRANSPORT=http)
MCP_PORT=3000

# Bearer token for HTTP auth (required when MCP_TRANSPORT=http)
# Generate a strong random token, e.g.: openssl rand -hex 32
MCP_AUTH_TOKEN=
